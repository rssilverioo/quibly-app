# Dependencies
node_modules/

# Build outputs
dist/
.next/
.turbo/

# Environment
.env
.env.local
.env.production

# IDE
.idea/
.vscode/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Prisma
apps/api/prisma/*.db

# Expo
apps/mobile/.expo/

# Misc
*.log
coverage/

# Estado do Maestri (papéis de agente) — ferramenta, não produto.
.maestri/
apps/web/tsconfig.tsbuildinfo

# Artefatos de build local do EAS (`eas build --local --output ./build/...`).
# O .ipa tem ~32MB e nasce fora de qualquer padrão já listado aqui — sem esta
# linha, um `git add -A apps/mobile` o commita inteiro sem avisar.
apps/mobile/build/
