[build]
buildCommand = "npm ci && npm run build -w packages/shared && cd apps/api && npx prisma generate && npx nest build"

[deploy]
startCommand = "cd apps/api && npx prisma migrate deploy && node dist/main.js"
healthcheckPath = "/"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
